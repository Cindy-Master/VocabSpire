# VocabSpire 更新日志

> 本文件是**唯一事实源**：发版时 manifest 描述、游戏内更新弹窗（ChangelogPopup.ChangelogLines）、
> GitHub Release notes、Steam changeNote 全部从对应版本段落派生，不得凭记忆另写。
> 未发布的改动先记在 [Unreleased]，发版时把该段定稿为 vX.Y.Z。

## v2.7.31（2026-08-26）

### 修复（重要 · 联机）
- **与其他 mod 同装时，联机答题同步会因 mod 加载顺序而失效甚至崩局**
  - 玩家反馈（游戏 v0.111.0，多人）：本 mod 必须排在 RitsuLib **前面**加载才正常，排后面就暴毙；而 RitsuLib 会把自己排到很前，于是必然踩中
  - 根因：答题结果（跳过效果 / 免费 / 回手 / 奖惩 / 重放）是追加在原生 `NetPlayCardAction` 数据包尾部的，靠「写入顺序 = 读取顺序」这个隐式位置约定对齐。而 Harmony 对同一方法的多个 Postfix 按 priority 排序，**只有 priority 相同才退化成按加载顺序** —— 本 mod 此前全部用默认优先级，于是「谁先加载谁先写」；一旦两端次序不一致，读端就会从错误的比特位开始解析：跳过效果/回手被误置、奖励数量读成离谱值，继而整包读爆
  - 修法三层，最终做到**完全不依赖加载顺序**：
    1. 收发两侧 Postfix 声明 `Priority.Last`，让两端次序一致
    2. 自定义段以 32 位魔数起头，读端用滑动窗口逐 bit 扫描定位 —— 别的 mod 插在前面、后面还是两边，都能找到自己的段
    3. 读完后把 `PacketReader` 的读取位置**原样还原** —— 即使本 mod 抢在别人前面读，排在后面的 mod 也照常读到自己的数据，不被破坏
  - 离线复刻比特流验证：mod 插在前 / 后 / 两边 / 前面塞 20 段，均正确对齐；读取顺序与写入顺序颠倒时，本 mod 扫描定位后其余 mod 数据完好无损；对端未装本 mod 时安全跳过
  - ⚠️ **联机协议有变更，双端必须同为 v2.7.31 及以上**

## v2.7.30（2026-08-26）

### 修复（重要）
- **答错回手在游戏 v0.109+ 上静默失效**（设置里开着、实际不生效，且不报错）
  - 根因：归堆方法名被游戏改了第三次 —— `GetResultPileTypeForCardPlay`（≤0.107）→ `GetResultPileTypeAndPositionForCardPlay`（0.108）→ **`GetResultLocationForCardPlay`**（0.109+，返回 `CardLocation`）。此前用「方法名含 ResultPileType」的模糊匹配来抗改名，而 0.109 恰好把这个词整段换掉了，匹配数归零
  - 改为按「精确方法名 + 精确返回类型」逐代匹配，并补上 0.109 形态的补丁；游戏再改名时会由挂载报告明确报出，不再静默降级

### 改进
- **新增补丁挂载报告**：启动时按功能打一张表（哪个功能、挂在哪个游戏方法上、命中几个），核心功能挂不上用 Error、可选功能降级用 Warn
  - 此前所有补丁共用一句同样的 Warn，核心失败和边缘失败看不出区别 —— 上面那个回手失效能潜伏几个版本，正是因为这个
  - 同一功能的多代兼容补丁按功能归组：任一代命中即算可用，全代落空才报故障（避免「三代里两代为 0」被误读成出错）

## v2.7.29（2026-08-26）

### 新增
- **内置两本医考选择题题库**：「26 临床执业医师历年真题（2007-2024）」5963 题 + 「贺银成执业医师题库（25 章节分类）」8832 题
  - 作者已于 2026-08-26 发布**永久免费授权**（可自由下载 / 使用 / 复制 / 分享，允许二次修改与再发布，须保留出处署名，禁止商业倒卖）；此前因来源是加密付费牌组，一直排除在发布包之外
  - 按授权要求补齐署名：两本的词库名与描述原本是文件名派生的乱码，现改为正式名称并注明出处（Kevin Choice Card / 贺银成执业 · Anki学习码头）与授权条款
  - 文件名（= 词库 id）保持不变，已在刷这两本的存档**进度不受影响**（进度按「词库id::单词」存）
  - 体检：14795 题、答案索引越界 0、空题干 0

## v2.7.28（2026-08-25）

### 新增
- **题型出题权重可调**（设置 → 题型区「按权重出题」）：每个题型 0-100 权重，例如拼写调到 20、其余 100，拼写出现频率就约为其他题型的 1/5
  - 此前题型是等概率随机，勾 4 个各 25%，想让拼写少出只能整个取消勾选 —— 要么全出要么不出
  - 总开关默认关 = 等概率（老行为不变）；权重 0 = 该题型不出；勾选仍是上界，没勾的题型权重再高也不出现
  - 实测 10 万次抽样：拼写 20 / 其余 100 → 拼写 4.7%、其余各 23.8%，与理论值 4.8% / 23.8% 吻合
- **手柄可操作答题面板**（设置 → 功能设置「🎮 手柄操作答题面板」，默认开）
  - 十字键 / 左摇杆上下移动选项，A 选中（再按一次提交），Y 直接提交多选，X =「忘了」
  - 回忆卡片：A 翻面 → 再按 A =「想起来了」，X =「没想起来」；答完题 A 继续
  - 拼写题需要打字，手柄只接管 X =「忘了」
  - 摇杆做了滞回去抖（推过 0.6 触发、回中 0.35 才能再触发），不会轻推一下连跳十几个选项
  - **有键位教程**：插着手柄时答题面板底部显示随题型变化的提示（选择题 / 回忆卡片翻面前后 / 拼写题 / 答完继续各不相同），没插手柄不显示、不占地方
  - 第一次检测到手柄时另弹一次引导条（顶部，7 秒后自动消失，只弹一次；与「按 F8 打开设置」的引导错开，不会叠在一起）
- **发音来源可选**（设置 → 听力音量下方，带「🔊 试听」按钮）
  - 自动（默认，老行为）：联网真人发音优先，失败回退系统合成音
  - 仅联网：只用真人发音，拿不到就静默（不想听机器音时用）
  - 仅本地：全程不发网络请求，断网 / 内网 / 在意隐私时用

### 修复
- **「忘了」按钮开关不持久化**：v2.7.27 加 `ShowForgotButton` 时漏了 ConfigData / Load / Save 三处，关掉后重启即复原

## v2.7.27（2026-08-12）

### 新增
- **回忆卡片题型**（墨墨背单词那种）：正面只给单词，自己在心里回忆意思 →「显示答案」翻面 → 自评「✅ 想起来了 / ❌ 没想起来」
  - 自评结果照常记入记忆引擎（想起来了 = 答对，没想起来 = 答错，升/降 Box 与重现间隔一致）
  - 音标随释义放在背面（放正面等于提前给线索，削弱主动回忆效果）
  - 战斗答题与**篝火复习**都支持；设置 → 题型区勾选，分层题型（Act1/2/3）与奖惩规则的「题型」筛选也已加入「回忆卡」
  - 固定选择题题库的题目不会出回忆卡片（题干+选项结构不适用）
  - 注：这是靠自觉的自评题（点「想起来了」就算对），因此不给难度奖励加成
- **「🤔 忘了」按钮**（选择题快捷键 `0`，拼写题同有；设置 → 功能设置可关）
  - 想不起来时直接认错并显示正确答案，不用瞎蒙
  - 动机：蒙对会让记忆引擎误判「已掌握」，主动认错的数据才准，这个词也会更快重现
  - 反馈文案为「没关系，记住它：X」而非「回答错误」；错题本记作「（忘了）/（没想起来）」
- **内置「高考大纲词汇3500（乱序版）」词库**（2012 词，音标覆盖 99%）
  - 来源：不背单词导出 PDF，用 `tools/pdf_to_wordbank.py` 转换（版式与专升本/雅思/托业同模板）
  - 校验：序号 1-2012 连续无缺号无重复、无空词条、页脚污染 0、断裂续行 0，并与 PDF 原文逐点核对首末词

### 修复
- **词库释义里的怪字**（`狭⻓地带`→`狭长地带`、`专⻔`→`专门`、`节⻝`→`节食`、`汽⻋`→`汽车`）
  - 根因：`⻓`(U+2ED3)、`⻋`(U+2ECB) 等 22 个「CJK 简体部首」字符没有兼容分解，NFKC 是恒等变换修不掉，原样落进了释义
  - 影响并已修复：专升本 558 处、托业 159 处（这两本随包发布，玩家可见）+ 雅思词汇真经 561 处（仅在仓库，未随包），共 1278 处
  - 转换工具补上映射表，未收录的同区字符不再静默通过而是告警
  - 纯字符替换、不重新序列化，`english` 字段未动 → **学习进度不受影响**（进度按「词库id::单词」存）
- 转换工具 `pdf_to_wordbank.py` 另两处解析缺陷：页脚检测长度阈值 18→40（书名超 19 字时会污染最后一页末词的释义）、释义硬换行被误当独立义项（实测 `than` 被切成两条）

## v2.7.26（2026-07-29）

### 修复（重要）
- **跨设备后学习进度冻结**（已掌握词汇量不再增长，删 mod 重装也无效）
  - 根因：每个词的 `DueTick`（第几题该复习）存在 `_word_progress.json`，而作为基准的时钟 `TotalAnswered` 存在 `vocabspire_config.json` —— **两个文件**。手动只拷进度文件、或重装 mod 导致配置被重置，两者就会错位
  - 实测用户档：时钟只有 36，而数据里 `DueTick` 已到 7942 → 所有词被判「还没到期」（权重压到 0.02 / 0.05），同时 51 个学习中的词超过新词节流上限 15 → 新词权重为 0 永不引入 → 掌握量彻底冻结
  - 修法：**加载进度后自动校验并自愈** —— 发现时钟落后于 `max(DueTick)` 就推进时钟（相对间隔不变，到期判定立刻恢复）；导出、导入两端也同样兜底修正，不再把坏时钟传播到别的设备
  - 真实档验证：99 个卡死在低权重的学习中词**全部恢复到期**
  - 对手机端（安卓无法访问文件、用不了导入按钮，只能手动拷 `_word_progress.json`）尤其有用：拷完直接进游戏即可，不必再手动同步配置

## v2.7.25（2026-07-26）

### 新增
- **跨设备学习进度导出 / 导入**（词汇图鉴 →「导出进度」「导入进度」，`.vsprog` 文件）
  - 修掉了直接拷 `_word_progress.json` 的三个坑：进度里 `bank` 与 `word` 分字段存（词库改名/重导可按单词兜底匹配，不再整个丢失）、随文件带上调度时钟 `TotalAnswered`（不必再手拷 `vocabspire_config.json`）、走游戏内导入并立即落盘（不会被内存旧数据覆盖）
  - 默认**合并模式**：掌握度（记忆盒 Box / 连对）取更靠前的一方，累计次数取较大值（不求和，避免虚高）→ 双端都在玩也不会互相冲掉
  - 导入前自动备份为 `_word_progress.backup_时间戳.json`，可回滚
  - 注：原有的「导出 CSV / JSON」是**错题本**（仅导错题，供打印复习），与进度转移无关

## v2.7.24（2026-07-26）

### 修复（重要）
- **CSV 导入的选择题答案错位**（表现为「选对判错、选错判对」）：数字答案改为**优先按 1-based** 解释（题库通行写法，如 `5` = 第 5 项 E），越界才退回 0-based，与 apkg 导入路径统一
  - 实测某 5966 题真题库：答案正确数 **58 → 3019**（可用解析文本校验的全部命中），因越界被整题丢弃 **1053 → 0**
  - ⚠️ 此前用 CSV 导入的题库需**删除后重新导入**才会修正；apkg / JSON 导入的题库不受影响
- **JSON 导入加同款容错**：`answer` 等于选项数时按 1-based 解释、支持字符串答案（`"B"` / `"3"`）；含 `options` 但答案无效时跳过该题，不再降级成垃圾词条（三个题库 14965 题验证：新旧逻辑结果零差异）

## v2.7.23（2026-07-26）

### 新增
- **CSV 选择题导入**：支持两种写法 —— 方案 A（`Options` 单列、`||` 分隔）与方案 B（`optionA`~`optionH` 分列）；答案列兼容 `answer`/`answers`，值支持字母（A-H）或数字索引
- **选择题选项上限 5 → 8**（A-H）：编辑器输入框、CSV 解析与答题 UI 按钮数对齐（JSON 数组本身无上限）
- **拉起卡牌时浮动显示连对/连错 + 奖励进度**：拖起手牌时在卡牌上方提示当前连对次数、各奖励规则还差几题触发（达标一次的显示 ✅ 已触发），松手消失
- 答题面板底部同时显示连对/连错与最近 2 条奖励规则进度
- 加密题库导入：AES 密钥本地缓存、导入失败弹窗显示具体原因

### 修复
- apkg 导入兼容性：支持 `WITHOUT ROWID` 表、旧版 anki2 加密牌组、`CREATE TABLE` 内 `/* */` 行内注释、JSON 转义引号
- apkg 导入失败时输出详细诊断日志（字段名 / 内容预览 / 是否密文 / 表结构）

## v2.7.22（2026-07-25）

### 新增
- Anki 加密题库解密导入支持（AnkiChinasDecryptor）
- ApkgImporter 集成加密解密 + 选择题题库格式自动识别

### 修复
- visitor ID 改为确定性设备指纹，避免浪费设备槽位

## v2.7.21（2026-07-25）

### 新增
- **固定选择题题库模式**：词库 json 词条支持 `options`（本题选项）+ `answer`（正确索引），`english` 存题干
  - 出题直接用本题自带选项，**每次出题选项乱序**（防背位置），不混入其他题内容
  - 记忆引擎 / 掌握度 / 篝火复习 / 词汇图鉴 / 多库合并 / 错题本全部兼容；发音类功能对中文题自动跳过
  - **可视化编辑完整支持**：每题 = 题干 + 正确答案下拉 + A-E 选项框；新建词库可切换「选择题题库模式」
- **内置「基础护理学选择题题库」**（170 题：出入院护理/医院感染与控制/职业防护/生命体征/清洁卫生）
- 新增 `tools/docx_quiz_to_json.py` 线下转换工具（Word 红色标答案/案例共用题干/内嵌选项，170/170 全解析）

## v2.7.20（2026-07-24）

### 新增
- **答完题自动朗读本题单词**：提交判定后自动播放发音，覆盖战斗选择题/拼写题与篝火复习；开关在设置 → 功能设置（默认开）
- **篝火复习「🔊 发音」按钮**：题干下方随时点随时读当前单词（答题前也可先听）

## v2.7.19（2026-07-24）

### 修复
- **v0.109 测试分支答错回手卡死**（表现为联机能力牌答错后卡在打出位、再也抽不到；单人在 0.109 同样会触发）：
  - 根因：v0.109 又更改了打牌归堆方法名，新旧两个归堆补丁都未挂载（`Patched 0`×2），回手标志设置后无人改归堆 → 能力牌走原生 `None` 被移出战斗，同时飞卡 VFX 被跳过致卡牌节点残留打出位
  - 归堆补丁改为**模糊匹配**（名字含 ResultPileType + 无参 + 按返回类型分派），游戏再改名也能挂上；0.107/0.108 命中集合与旧精确匹配逐一核对一致
  - 新增保护层：归堆补丁未挂载时回手功能整体安全禁用（牌走正常结果堆 + 日志 Warn），杜绝「卡中间 + 抽不到」半残态
  - 联机标志同步经日志实证工作正常（含 0.108 struct 化后），与本 bug 无关

### 新增
- 设置面板标题栏 ✕ 关闭按钮（不再只能靠 Esc）

## v2.7.18（2026-07-23）

### 新增
- **界面字体大小可调**：设置面板「功能设置」新增五档字体缩放（小 85% / 标准 100% / 大 115% / 特大 130% / 超大 145%），作用于设置面板与答题面板；改动即时生效、重启保持

## v2.7.17（2026-07-15）

### 修复
- **兼容游戏 v0.108 测试版（含手机移植版），修复「DLL 加载失败」**：
  - 根因：0.108 把 `GetResultPileTypeForCardPlay` 改名为 `GetResultPileTypeAndPositionForCardPlay` 且返回 `(PileType, CardPilePosition)` 元组 → 旧补丁 TargetMethods 为空 → Harmony 对空 targets 抛异常（不是跳过）→ PatchAll 炸掉整个 mod 初始化
  - Harmony 补丁改为**逐类隔离挂载**：单个补丁失败只禁用对应功能并打 Warn，mod 其余功能不受影响（未来游戏更新也不再整体挂掉）
  - 新增 `GetResultPileTypeAndPositionPatch`（0.108+ 元组版，基类+3 子类 override），与旧补丁（≤0.107）共存、按版本各自生效 → **答错回手在 0.107 正式版与 0.108 测试版均可用**
  - 已在 0.108 实测：初始化完整、回手正常（`Patched 4 GetResultPileTypeAndPosition... (v0.108+)`）

## v2.7.16（2026-07-15）

### 修复
- 更新弹窗重做并启用：改用游戏官方弹窗组件（NGenericPopup/NVerticalPopup，原生样式），延迟到主菜单就绪后弹
  - 首版自绘面板在主菜单渲染成全屏黑条 → 已废弃
  - 二版经 NModalContainer.Add 挂载时其内部 ActiveScreenContext.Instance 在主菜单为 null 抛 NRE（被内联致行号误指），导致文本未覆盖显示成「退出」占位 → 改为直接挂 UI 根 + YesButton 自行接 Released 信号
- （2.7.15 发布当天曾紧急撤回弹窗，工坊版弹窗为禁用状态；本版恢复）

## v2.7.15（2026-07-15）

### 新增
- 内置「托业 TOEIC 核心词汇」词库（1633 词，音标覆盖 99%）
- 「出多选题」开关：默认开；关掉后多义词也只出单选（设置 → 功能区）
- 「选项发音小喇叭」的设置开关（功能 2.7.14 已上线，本版补上可关闭的 UI）
- 进游戏版本更新弹窗：每版首次进游戏弹一次本版更新要点 + QQ 交流群 750809524 + 开源地址

## v2.7.14（2026-07-07）

### 新增
- 词汇图鉴改分页渲染（每页 60 行 + 翻页），多词库合并上万词不再卡死
- 「重放本牌」奖励回归：答对时用游戏原生机制把该牌额外再打 N 次（含能力牌），单机 + 联机（NetPlayCardAction 同步两端 playCount）；补丁防御式（方法不存在则禁用不崩）
- 中→英题每个英文选项加 🔊 小喇叭，点击听该选项发音（复用 TTS）

## v2.7.13（2026-07-05）

### 修复
- 能力牌答错回手变空白牌：回手时跳过飞卡 VFX、保留牌面
- Sly（奇巧「被丢弃时自动打出」）等自动打出的牌答错凭空多一张：自动打出的牌答错不再回手，走正常结果堆

### 新增
- 奖励 + 惩罚规则「适用题型」筛选（拼写题→+力量、听力题→+覆甲等按题型定制）
- 同时激活多个词库：多选勾选、按英文去重合并出题、释义合并、进度归激活顺序第一个库
- 连对/连错高级机制（每条规则独立、奖惩对称）：重算范围（永久/每场战斗/每回合）+ 冷却（隔 X 题）+ 计数封顶 + 本周期最多触发次数
- UI 加固：编辑词库 PopupMenu 懒创建；CreateUI 逐面板隔离

> 注：2.7.11/2.7.12 的重放功能曾因误判回退（实为游戏 D3D12 无焦点启动崩溃，与 mod 无关），2.7.14 已回归。

## v2.7.10（2026-07-03）

### 修复
- 「编辑当前词库」打开大词库（几千词）卡死：改为分页渲染（每页 50 行 + 翻页）

## 更早版本

见 `VocabSpire/VocabSpire.json` 的 description 逐版日志，或 GitHub Releases 页。
